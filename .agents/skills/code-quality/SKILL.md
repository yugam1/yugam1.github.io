# Code Quality
TypeScript strict mode — no any types
After every feature run: npm run build. Fix errors before done.
Tailwind only — no inline styles
Components in src/components/, sections in src/sections/